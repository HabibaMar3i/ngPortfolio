export interface Project {
    id?: number;
    title?: string; 
    description?: string;
    tools?: string[];
    features?: string[];
    githubLink?: string;
    liveLink?: string;
    imageUrl?: string;
}
